services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: lightweight_cms
      POSTGRES_USER: cms
      POSTGRES_PASSWORD: cms_dev_password
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
